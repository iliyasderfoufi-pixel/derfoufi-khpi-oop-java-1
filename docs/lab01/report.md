\# Laboratory Work No. 1 Report



\- \*\*Topic:\*\* Environment Setup and the First Java Program

\- \*\*Prepared by:\*\* Ilyas Derfoufi

\- \*\*Instructor:\*\* Володимир Миколайович Савченко

\- \*\*City:\*\* Kharkiv

\- \*\*Year:\*\* 2026



\## Environment Setup



| Component | Version |

|---|---|

| OS | Windows 11 |

| JDK | Oracle JDK 21.0.12.1 |

| Git | 2.55.0.windows.5 |

| IntelliJ IDEA | 2026.2 |



\- \*\*Repository:\*\* `derfoufi-khpi-oop-java-1`

\- \*\*Branch:\*\* `lab01`

\- \*\*Pull Request:\*\* To be added after publication

## Purpose

The purpose of this laboratory work is to set up the Java development environment, create and run a first Java program manually using JDK 21, inspect the generated bytecode, reproduce and correct a compiler diagnostic, and compare the manual workflow with IntelliJ IDEA.

## Variant (Assignment Description)

- **Variant:** common
- **Assignment:** Set up the Java development environment, create and run the first Java program, inspect the generated bytecode, reproduce and correct a compiler diagnostic, create an environment report, and verify the workflow using IntelliJ IDEA and Git.
- **Input data:** Java source files and the local development environment.
- **Expected result:** Both Java classes compile and run successfully, the bytecode can be inspected, the controlled syntax error produces a compiler diagnostic and is corrected, and the project can be reproduced and verified through the command line and IntelliJ IDEA.


## Problem Analysis

- **Inputs:** Java source files and the local Java development environment.
- **Outputs:** Compiled `.class` files, console output from both Java classes, compiler diagnostic output for the controlled syntax error, and verification evidence.
- **Assumptions:** JDK 21, Git, and IntelliJ IDEA are installed and available for the laboratory work.
- **Constraints:** The project must compile and run with JDK 21 without Maven or Gradle. Derived files such as `out/`, `.class` files, and IDE service directories must remain outside Git tracking.
- **Boundary cases:** A missing semicolon in `HelloJava.java` must produce a compiler diagnostic; after correcting the syntax error, the program must compile and run successfully.
- **Solution plan:** Verify the environment, create the required project structure and Java classes, compile and run the programs manually, inspect the bytecode, reproduce and correct a syntax error, run the classes in IntelliJ IDEA, perform the required debugging session, and verify the Git repository state.


## Class Structure

### Directory Tree

```text
derfoufi-khpi-oop-java-1/
├── .gitignore
├── README.md
├── docs/
│   └── lab01/
│       ├── report.md
│       └── assets/
├── src/
│   └── ua/
│       └── khpi/
│           └── oop1/
│               └── lab01/
│                   ├── HelloJava.java
│                   └── EnvironmentReport.java
└── out/  # ignored by Git



## Algorithms and Program Description

- **Algorithm:**
  1. Verify the installed JDK, Git, and operating system.
  2. Create the Java source files in the required package.
  3. Compile `HelloJava.java` with `javac`.
  4. Run the compiled class with `java` using the output directory as the classpath.
  5. Inspect the generated bytecode with `javap -c`.
  6. Introduce a controlled syntax error in `HelloJava.java` and observe the compiler diagnostic.
  7. Correct the error and compile and run the program again.
  8. Run `EnvironmentReport` from IntelliJ IDEA and verify the environment information.
  9. Debug `EnvironmentReport` using a breakpoint, Step Over, and Resume Program.

- **Key fragments:**
  The `HelloJava` class demonstrates the basic Java program structure and prints a greeting. The `EnvironmentReport` class uses Java system properties to display information about the Java runtime and operating system.

- **Compilation and launch:**
  ```powershell
  javac -d out src\ua\khpi\oop1\lab01\HelloJava.java
  java -cp out ua.khpi.oop1.lab01.HelloJava



## Testing and Test-Data Selection

- **Test data:**
  - Java version: `21.0.12.1`
  - Java vendor: `Oracle Corporation`
  - Operating system: `Windows 11`
  - OS version: `10.0`
  - Working directory: `C:\Users\scoot\OneDrive\Bureau\derfoufi-khpi-oop-java-1`
  - Test program: `HelloJava`
  - Environment program: `EnvironmentReport`

- **Results:**

| Test | Expected result | Actual result |
|---|---|---|
| `javac -d out HelloJava.java` | Compilation succeeds | Passed |
| `java -cp out ua.khpi.oop1.lab01.HelloJava` | `Hello, Java!` is printed | Passed |
| `javap -classpath out -c ua.khpi.oop1.lab01.HelloJava` | Bytecode is displayed | Passed |
| Missing semicolon | Compiler reports a syntax error | `';' expected` |
| Corrected `HelloJava.java` | Compilation and execution succeed | Passed |
| `EnvironmentReport` in IntelliJ IDEA | Environment information is displayed | Passed |
| Debug breakpoint | Execution stops at the selected line | Passed |
| Step Over | Execution moves to the next statement | Passed |
| Resume Program | Program continues to completion | Passed |

- **Debugging:** A breakpoint was placed on the first `System.out.println` statement in `EnvironmentReport`. The local variables were inspected in the Debug window. Step Over (`F8`) executed the current statement and moved execution to the next statement. Resume Program continued execution until the program finished with exit code `0`.

- **Git evidence:** `git status` reported a clean working tree after the laboratory steps. The `out/` directory and `.class` files were excluded from Git tracking through `.gitignore`.


## Conclusions

The laboratory work was completed successfully. The Java development environment was configured with JDK 21, the first Java program was compiled and executed from the command line, and the generated bytecode was inspected with `javap`. A controlled compiler error was reproduced and corrected. The programs were also executed and debugged in IntelliJ IDEA using a breakpoint, Step Over, and Resume Program. Git was configured to track the source and report files while excluding generated build files such as `out/` and `.class` files.

